# Copy to .env.local and fill in your key
SERPAPI_KEY=
# Member login (server-only — never exposed to the browser).
# Logged-in visitors use the server keys with no trial limits.
SAM_ID=sam
SAM_PW=1227
